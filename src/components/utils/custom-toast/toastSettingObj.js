import { toast } from 'react-toastify' //allow to use enum to set positon

export default {
  position: toast.POSITION.TOP_RIGHT,
  className: 'custom-toast',
  progressClassName: 'custom-toast-progress',
}
