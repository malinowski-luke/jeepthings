import React from 'react'
import { connect } from 'react-redux'
import './Form.css'

function Form() {
  return <div className='Form'>Form.js</div>
}

// const mapStateToProps = state => ({})

// const mapDispatchToProps = {}

export default connect(null, {})(Form)
